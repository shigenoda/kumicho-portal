# App Configuration
VITE_APP_ID=your-app-id

# Security
JWT_SECRET=your-jwt-secret-key-here

# Database (MySQL)
# For local development: mysql://user:password@localhost:3306/database
# For PlanetScale: mysql://user:password@host.connect.psdb.cloud/database?sslaccept=strict
DATABASE_URL=mysql://user:password@host:port/database

# OAuth (Optional - remove if not using OAuth)
OAUTH_SERVER_URL=https://your-oauth-server.com
OWNER_OPEN_ID=owner-open-id

# AI/LLM API (Optional - remove if not using)
BUILT_IN_FORGE_API_URL=https://api.example.com
BUILT_IN_FORGE_API_KEY=your-api-key-here

# Environment
NODE_ENV=production
